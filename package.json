# Backup old package.json
cp package.json package.json.bak

# Create new package.json
cat > package.json << 'EOF'
{
  "name": "crush-ray-bot",
  "version": "1.0.0",
  "description": "💖 CRUSH RAY BOT - WhatsApp Bot for Render",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "author": "PRESENTER RAY",
  "license": "ISC",
  "dependencies": {
    "@whiskeysockets/baileys": "^6.7.0",
    "express": "^4.18.2",
    "socket.io": "^4.7.2",
    "qrcode": "^1.5.3",
    "pino": "^8.21.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
EOF

# Verify the file
cat package.json
